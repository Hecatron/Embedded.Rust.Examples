[package]
name = "microbit-13-nostd"
edition = "2018"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7.5"
panic-halt = "1.0.0"
defmt-rtt = "0.4.0"
defmt = "0.3.10"
embedded-hal = "1.0.0"

# Microbit V1
microbit = { version = "0.15.1" }
