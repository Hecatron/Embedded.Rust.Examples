[package]
name = "testapp"
version = "0.1.0"
authors = ["grbd <hecatrons.workshop@gmail.com>"]
edition = "2018"

[[bin]]
name = "testapp"

#[dependencies]
#libc = { version = "0.2.85", default-features = false }

[dependencies]
globwalk = "0.8.1"
bindgen = "0.57.0"
